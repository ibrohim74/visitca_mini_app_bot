import React from 'react';

const HomePage = () => {
    return (
        <div>
            home
            <button onClick={localStorage.clear}>ufgf</button>
        </div>
    );
};

export default HomePage;